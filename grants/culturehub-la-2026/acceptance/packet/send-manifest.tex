% !TEX program = xelatex
% Internal checklist only. Do not send this document to CultureHub.
\documentclass[11pt,letterpaper]{article}
\usepackage{culturehub-packet}
\renewcommand{\packetlabel}{INTERNAL · DO NOT SEND}
\hypersetup{pdftitle={CultureHub LA — Send Manifest}}

\begin{document}
\packettitle{CultureHub LA}{What to send now · what to hold}

\section{Send by August 11}
\statusready{READY AFTER FINAL VISUAL APPROVAL}\par
\begin{enumerate}
\item \textbf{Artist-page copy:} attach \texttt{artist-page-packet.pdf}, or paste its bio, project statement, and links into the email.
\item \textbf{Headshot:} attach \texttt{jeffrey-alan-scudder-headshot-3x2-green-laptop.jpg} --- the chosen portrait. Offer the animated \texttt{.webp} and its still as alternates only if CultureHub asks for a plain headshot.
\item \textbf{Superseded:} \texttt{jeffrey-alan-scudder-headshot-3x2-animated.webp}. Also attach the GIF and still fallbacks if CultureHub’s CMS requirements are unknown. PDF preview uses the smiling still because PDF cannot animate.
\item \textbf{Project image:} attach \texttt{marketing/campaigns/macneopolitan/gens/program.png} (1536×1024). It carries both works --- three MacBook Neos across the front, the six salvaged laptops ringed behind. It replaces the interim \texttt{special-sign-live-spatial-3x2.jpg}.
\item \textbf{Credits and alt text:} use the exact lines printed beneath each image in the artist-page packet.
\end{enumerate}

\subsection{Program change --- August 9, 2026}
The program is now \textbf{two works}, billed \emph{Whistlegraph presents}: \emph{Special Sign} (six salvaged laptops, AC Native, notepat) and \emph{MacNeoPolitan} (three MacBook Neos --- indigo, citrus, blush --- each played through Menu Band). \texttt{ARTIST-PAGE.md} and \texttt{EVENT-PAGE.md} were rewritten to match; the \texttt{.tex} packets below still carry the older single-work copy and must be regenerated before they are sent.

\textbf{Never write that AC Native runs on Apple hardware.} AC Native is x86\_64 UEFI only; \emph{MacNeoPolitan} runs Menu Band, a shipping universal macOS app.

\section{Hold until confirmed}
\statushold{DO NOT SEND AS FINAL YET}\par
\begin{itemize}
\item \texttt{event-page-packet.pdf} — dates, times, livestream, and final title remain provisional.
\item \texttt{technical-rider.pdf} — send for the production conversation after CultureHub names its technical contact; the Kalio sixth-channel path must be verified.
\item \texttt{SCHEDULE.md} — internal working calendar, not a public attachment.
\end{itemize}

\section{Decisions still open}
\begin{itemize}
\item Running order of the two works, and the final billing line beneath \emph{Whistlegraph presents}.
\item Purchase of the third MacBook Neo (blush). Do not describe its finish in copy until the machine is in hand.
\item Jeffrey’s approval of the spatial still as the interim project image.
\item CultureHub’s accepted image formats and whether animated WebP is supported.
\item Confirmed public-program dates, times, and whether Menu Band Jam is a separate offering.
\item Room, signal, network, projection, streaming, and documentation specifications.
\end{itemize}

\palsfooter
\credit{Internal assembly guide · prepared 3 August 2026.}
\end{document}
