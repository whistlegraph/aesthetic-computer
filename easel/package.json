{
  "name": "easel",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node bin/sync-context.mjs --check && node --test test/*.test.mjs && bash test/cli.sh",
    "context": "node bin/sync-context.mjs"
  }
}
