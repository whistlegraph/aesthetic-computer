# Supply-chain cooldown — see the repo-root .npmrc for the full rationale.
# npm reads .npmrc from the install's own local prefix and does not walk up, so
# this has to be repeated in every directory `lith/deploy.fish` installs from.
# Requires npm >= 11.10; silently ignored on older npm.
min-release-age=7
