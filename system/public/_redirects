# TODO: This file should be obsolete now that `netlify.toml` has routing.
# Redirects from what the browser requests to what netlify serves.
# Use clean urls that I can route via the client.
# See: https://www.netlify.com/blog/2019/01/16/redirect-rules-for-all-how-to-configure-redirects-for-your-static-site

# *            /split.html     200
# /thumbnail/* /.netlify/builders/thumbnail 200
# /*           /.netlify/builders/index     200