// Dolls, 23.02.10.14.46 
// Make cute 2D cartoon videos with the microphone! 

/* #region ✏️ notes 
- Dolls - (Record Character)
- Character 1 (Left)
  - Blob
- Character 2 (Right)
  - Blob
- Single background for both.
- Third thing (Subject of dialogue).
- Duration that fits the vibe. 
- Vocal Effects?
#endregion */

// 🥾 Boot (Runs once before first paint and sim)
function boot($) {
}

// 🎨 Paint (Executes every display frame)
function paint() {
}

/*
// ✒ Act (Runs once per user interaction)
function act({ event }) {
  // Respond to user input here.
}

// 🧮 Sim(ulate) (Runs once per logic frame (120fps locked)).
function sim($api) {
  // Crunch numbers outside of rendering here.
}

// 💗 Beat (Runs once per bpm, starting when the audio engine is activated.)
function beat($api) {
  // Make sound here.
}

// 👋 Leave (Runs once before the piece is unloaded)
function leave($api) {
  // Pass data to the next piece here.
}
*/

// 📚 Library (Useful functions used throughout the piece)
// ...

export { boot, paint }