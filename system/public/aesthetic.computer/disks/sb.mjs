// sb, 23.04.13.14.29 
// This piece is a meta-router / shortcut for "shillball".

// 🥾 Boot (Runs once before first paint and sim)
export function boot({ params, jump, store }) {
  jump("https://galerie-yechelange.baby/ball");
}