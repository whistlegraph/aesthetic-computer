// Amby, 2024.7.26.02.07.50.093
// A tonal music generator using radial cycles and turtle graphics.

/* 📝 Notes
   - [] Add circular cycles that play notes in the scale.
   - [] Visualize the tones and relate the sizes to the sounds.
*/

let a1 = 0;
let a2 = 180;

function paint({ wipe, ink, line, goto }) {
goto();
}

// 📚 Library

// function boot() {
 // Runs once at the start.
// }

// function act({ event: e }) {
//  // Respond to user input here.
// }

// function sim() {
//  // Runs once per logic frame. (120fps locked.)
// }

// function beat() {
//   // Runs once per metronomic BPM.
// }

// function leave() {
//  // Runs once before the piece is unloaded.
// }

// function preview({ ink, wipe }) {
// Render a custom thumbnail image.
// }

// function icon() {
// Render an application icon, aka favicon.
// }

// ⚠️ Also available: `brush` and `filter`.
