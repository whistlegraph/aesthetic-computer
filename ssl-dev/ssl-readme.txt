
1. Install mkcert with homebrew.

2. `cd` into /ssl-dev directory and run 'mkcert --cert-file localhost.pem --key-file localhost-key.pem localhost aesthetic.local 127.0.0.1 0.0.0.0'

3. Change generated certificate names to localhost-key.pem and localhost.pem 
