# Nelamesh Scope

The macOS client for seeing services available to the NELA Computer Club mesh.

The first slice observes a conservative set of ports on `localhost` and opens
recognized HTTP services. It does not expose, proxy, or share any service.

```bash
swift run
```
