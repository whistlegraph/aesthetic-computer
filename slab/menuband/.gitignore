.build/
.build-debug/
.build-appstore/
.build-rc/
.swiftpm/
DerivedData/
build/plugin-appstore-derived/
build/plugin-dev-derived/
AppIcon.iconset/
*.xcodeproj/
*.swp
.DS_Store
# Build artifacts — distribute the DMG via DO Spaces (system/public/assets
# is the sync source) instead of committing it.
*.dmg
