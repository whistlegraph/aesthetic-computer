# render outputs — large + deterministic from the score + bin scripts
out/

# decoded / pitched audio derivatives — regenerated at render time from
# their source .mp3 siblings (ffmpeg + rubberband caches)
assets/*.decoded.wav
assets/*.p+*.wav
assets/*.p-*.wav

# recorded voice takes — RFA captures live outside the repo
voice-takes/*.wav
