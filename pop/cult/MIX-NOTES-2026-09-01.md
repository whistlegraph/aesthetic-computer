# wannadash — mix notes, 2026-09-01

@jeffrey's notes on `out/wannadash-release.flac` (the 18:51 cut), taken while
watching `out/wannadash-release-review.mp4`. Timecodes are the **final edit**;
bar numbers are the **score** in `bin/render10.mjs`, mapped through
`bin/cut-release.sh`.

## 1. The opening is too extreme — the phone sounds

> "its nice but the opening i think is too extreme … with all the phone sounds
> … i wish we could get it to be more subtle / better mixed in"

The final's first seconds are two score fragments, not one:

- **0:00–0:04** = source 15.95–20.0 s = **bars 8–10** — `phone-pickup-a`
  (`bus: "vox"`, gain 0.20) plus the first of the three voices.
- **0:04 onward** = source 58 s = **bar 29**, the two bars of phone harmonics
  that set up the first "run real fast" at bar 31.

So the listener meets the record through the phone layer twice over, back to
back, with the edit removing everything (bars 10–29) that would have made it a
gradual arrival. Candidates: the bar-8 pickup gain, the bar-29/30 phone
harmonics, and the SIGNAL bus half-depth duck that is supposed to seat the
phone under the bed.

## 2. Bass and guitar attack/decay around 1:12 — "marching band heavy"

> "the attack / decay of the bass notes especilly / guitar is a little marching
> band heavy / weird around 1:12"

1:12 maps to source ~133.95 s = **bar ~67**, early in `spread: [64, 76]`.
What is playing there:

- `guitar-chug` — the palm-muted B-pedal, `gb = 64` with `dur: 7.45`, so one
  held chug covers bars 64–67.7 (render10.mjs:1802–1810).
- `guitarChord` — square block chords every 4 bars on the Bm/D/G/Em cycle,
  `dur: 3.25`, `gb = 64` (render10.mjs:1826).

A pedal chug plus downbeat block chords with no inner motion is exactly the
"marching band" read.

## 3. The guitar there should be more flowery / jazzy

> "i wish the guyitar there was more flowery / jazzzy"

Same place as note 2. The vocabulary already exists in the lane —
`guitarShred()` (render10.mjs:485) does bowed runs, and `guitarChord()` takes
an `up` flag for rake direction — but bar 64–68 uses neither. Nothing voices
the chords beyond root-position four-note stacks (the `GC` table,
render10.mjs:1818–1823): no extensions, no passing tones.

## 4. Attenuative dip before the spatial explosions

> "before the explosions / spatial explositions there is liek an attenuative
> dip … id prefer it was more smooth transition thewre"

The `blast` explosion is `bar: 76` → final **1:22** (this is the "bus-dispersed
1:23 blast"). Suspected mechanism, **not yet confirmed by measurement**:
`elasticizeBus()` (render10.mjs:2633+) attenuates by
`distance = 1 / (1 + 0.24 * travel)` and never boosts, so the whole bus can
only get quieter as the body travels; combined with `attack`/`release` edges
and `phraseLevel` breathing into the section change, the approach reads as a
duck instead of a lift.

## Status

- `prox:bot`'s session ended some time before 19:30 — its Terminal window is
  gone and `prox list` shows only this session on blueberry. These notes were
  spoken to it but landed here.
- The DistroKid form was lost in a Chrome crash and needs rebuilding
  (`dk-rebuild.sh` in the session scratchpad).
- `bin/render10.mjs` carries 381 lines of bot's uncommitted work. Nothing here
  has been changed yet.

---

# Round two

Notes taken while listening to `wannadash-notes-review.mp4`. All implemented
in `bin/render10.mjs` unless marked otherwise.

| # | note | what was done |
|---|---|---|
| 5 | "sharpen / brighten jeffrey's vocal … they sound smooth but i want sharper cutting … in like the dashes and run real fast" | New `bright` option on `shot()` — a one-pole high shelf (~3.2 kHz), `dark`'s missing opposite. His dash loses its 0.22 lowpass and takes `bright: 0.95` with a faster front; the syllabic "run real fast" take gets 1.05; his bassdash 0.55. His sub double keeps its darkness — that layer is the floor. |
| 6 | "for the long cult more harmonies would be good" | Two takes was a duet. Added the bottom pick entering last and widest, plus the top take an octave up as air. Four voices now. |
| 7 | "the cplostions should be sharper crisper louder … they sort of blur things out … i'd prefer more a sharper slide feel" | `glitch` (the grain smear that was doing most of the work) cut roughly in half across the table; `strength` up; attacks shortened to 30 ms on the blast and shatter; the propagation-delay coefficient widened 0.007/0.012 → 0.012/0.021 so the *slide* is longer than the smear sitting on it. |
| 8 | "if the electrig guityar had actual twanging / strumming pattern that'd be nice" | New `guitarStrum()` — down strums rake the whole voicing, up strums catch only the upper strings, faster, shorter, quieter. The finale's one-rake-per-two-bars became a hand playing every bar, odd bars taking the pattern's second half. |
| 9 | "bringing in white noise / air … a bit of hiss … especially on the overall chant sections and for the overall material feel" | New `air()` — decorrelated per-channel wind + hiss, on its own RNG so it does not shift the record's jit() stream. Four-bar overlapping tiles from bar 8, level following the acts, widest under the spread. |
| 10 | "bring a camille voice in for some 'daaaaaash' … not jiust alex and jeffrey leading but both" | She opens the bridge now instead of only answering: `raga()` (her `dashlong` bank, deep slow bend) at k=0, and again answering Alex at k=4. |
| 12 | "cool spatial illusion audio tricks with stero panning … illusiory pann efects … like staircase effects?" | New `staircasePan()` — the Shepard tone's spatial cousin: three staggered copies, each windowed silent at both edges, so the word appears to keep sliding one way forever. The ALT_CULTS chants ride it, direction flipping per bar. |
| 13 | "a long running fem gong bell at a high frequency … side chained to thew kicks … continuous tone for the first like 24 seconds … like a dinner bell was stricken" | B6 (1975.5 Hz) off the FEM bell engine — bronze, handbell, 26 s → `samples/gong-b6.wav`. On the **music** bus, which is the one `bedEnv` ducks from the kick, so the sidechain is the bus rather than an added effect. Struck twice (bar 8 and the bar-29 seam) because the edit cuts between them. |
| 15 | "the intiial opening sound … can be moved to that intiial drop too" | The waterhole ding returns on the bar-31 "run real fast" — quieter and wider than the opening strike. |
| 16 | "we sort of lose the tempo on the explosition at 1:07 … up the tempo during the warble … keep the energy driving forward" | Two parts. `keepTime` grounds the DRUM bus during the long explosions (0.30 on the bar-64 unfurl, 0.45 on gravity) so there is a fixed floor to hear the gesture against. And because the grid is a fixed 120 that cannot accelerate without re-performing everything, the *density* climbs instead: hats 8th → 16th with doubled offbeats across bars 64–67, a pushed kick ahead of beats 2 and 4, the whole figure creeping earlier each bar. |

## Open

- **11 · the stamp, "voiced in an angelic camille voice".** The ident already
  existed (`shot("dotorg", 18 * BAR …)`) and had *never played on the shipped
  record* — bar 18 is inside the twenty bars the release edit removes. It is
  now placed a second time at bar 8, in the first bar of the cut. But the take
  is **jeffrey's PVC** snapped to B2/F#2/B1. There is no Camille voice clone in
  the stack: `/api/say` has jeffrey's PVC plus generic ElevenLabs voices
  (Sarah / Laura / Jessica), and no Camille model exists. Needs @jeffrey's
  call — generic female TTS, a real Camille recording, or keep jeffrey's.
- **14 · "alex's 'cult' ones are too normal … should be more weird, pitched
  around, bunched up, around 1:25."** Not started. 1:25 lands at bar ~77.5, in
  the `whole` act.
- The two bar-end dips at 1:19.9 / 1:20.9 from round one — still there, still
  the dot field being sparse on purpose.

---

# Round three

| # | note | what was done |
|---|---|---|
| 17 | "after the click sample lets cut the chant right away at the end of the track" | The bar-110 chant stops a beat and a half after the bop instead of ringing four seconds past it. |
| 18 | "the hiss feels like a little loud … or not sidechained / affected enough by the other elements" | The air moved to its own bus. On the signal bus it sat in bedEnv^0.5 — a half-depth breath that never got out of the way. It now rides pumpEnv^1.35, the tube's deep kick+snare pump, and its gain dropped 0.0135 → 0.0085 (−4 dB). |

## The C engine

`c/cultremix.c` was five commits behind the JS and had never carried the vox
macro arc. Re-ported to the current score and verified sample-exact
(residual 1.8e-7; see `c/README.md`). `bin/bake-c.sh` renders and cuts the
DistroKid master in ~11 s instead of ~25 s. The review video still needs the
Node render for its receipt.

---

# Round four (on the C lane)

| # | note | what was done |
|---|---|---|
| 19 | "any way we can get the jeffrey voice sharper" | New `edge` on `shot()` — a second, higher shelf (~6.6 kHz) that lifts the tip of a consonant where `bright` lifts its body — and `arcDark: false`, which lets a take keep the vox arc's gain ramp but refuse its early darkness floor. His lead dash: bright 0.95→1.25, edge 0.6, arc darkness declined. The syllabic "run real fast" (chorus and hook): bright 1.2, edge 0.5, arc declined. The sub floors never inherit any of it. |
| 20 | "1:07–1:08 is a bit weird, lots of weird percussion hits, weird transition" | Three things were landing on the release edit's bar-63.88 seam at once. Bar 63's open hat and 32nd roll — a fill the edit cut in half — are dropped. The drive under the warble no longer starts on the seam: it begins at bar 65, softer (0.055 + 0.020k). The unfurl's `keepTime` on the drums 0.30→0.15. |
| 21 | "some 'i wanna' parts could be slower / stretched out, 'wannnnnnnaaaaaa' at times, and could bleed into the dash" | Every other hook and chorus statement, the pickup is the granular stretch instead of the take — 2.3–2.4× its length, dark 0.12 — so the vowel is still going when the dash lands and the two overlap for most of a second. The other statements keep the plain take, so "at times" stays true. |
| 22 | "the final click sample should cut the track, no final harmony there should end it" | `cut-release.sh` now trims [e] 180 ms past the second synth click with a 30 ms fade. The hang-up clunk under the clicks survives; the busy signal after them is gone. 141.25 s → 138.85 s. |

Both engines still sample-exact after all four (residual 1.2e-7).

---

# Round five (aesthetivox)

| # | note | what was done |
|---|---|---|
| 23 | "can the whistlegraph dot org stamp be stretched and aesthetivoxed" | `sung/dotorg-long.wav` — the ident through the WORLD chain (`bin/sing.py`), B2/F#2/B1 held 1.0/0.8/1.6 s: 1.53 s → 3.40 s, vowels sustained, consonants left alone. Both bar-8 placements (the ident and its octave angel) use it; the plain take is the fallback. |
| 24 | "the i wanna that's stretched is not smoothed out enough" | Same lesson as the opening dots: the granular stretch shows its grains. `sung/iwannalong-a` and `-c` are the takes re-sung through the WORLD chain (D4/E4 and B3/C#4, 0.55/1.15 s — 0.5 s → 1.7 s). They replace the stretcher on the alternate statements; the stretcher is now only the fallback. |
| 25 | "lengthening some faaaaasssts could be nice too" | `sung/runrealfast-fastlong.wav` — the syllabic take with its last vowel held 1.7 s (G4/F#4/D4, 2.0 s → 2.32 s). On the same alternation as the long i-wannas, it stands in for the syllabic take in the chorus and the hook, with the same shelves. |

---

# Round six

| # | note | what was done |
|---|---|---|
| 26 | "the dot org is hard to hear" | It was on the tube bus — the deep kick+snare pump — under the opening kick ramp, swallowed every beat. The drum bus never ducks (that is why the ding rode it); the ident rides it now at 0.66 with the shelves, and the gong under it gives up a little room (0.22→0.18). |
| 27 | "if the wanna is slow the dash should be slow too, and still be present" | `dashStack(…, slow)` swaps the 1.5 s holds for the 4 s `dashlong-*` renders — Camille at pitch, Jeffrey in his own octave, Alex where his one long take fits — held 3.0 s (hook) / 2.6 s (chorus), the stack lifted 18%. Follows the long i-wannas on the same alternation. |
| 28 | "can the cuuuult chants come in more from a distance … running towards and away from the microphone" | New `run` on `shot()`: `far` falls from 1 to 0 at the mic and rises again; far is quieter (−74%), darker and wetter, and the pitch bends up on the approach, down on the way out — a gentle Doppler (±26 cents). The three choir voices run staggered (phase 0.05 + 0.28i), the sub floor gently, Camille's act-IV takes too. |
| 29 | "a soft kick through the chants too, to sustain it" | Four on the floor at a quarter weight through the secret act and the walk-out (fading), fixed times so the rnd() stream holds. |
| 30 | "the 'cult' parts around 1:16–1:19 are too plain, deeper / stretched out more" | The bridge staircase cults drop a fifth (which also makes them half again as long), with an octave-down shadow crossing the other way underneath. |
| 31 | "a fibonaccian dot dot dot structure, thousands of dot dots divided like a field shredding, then explode those — the finale, towards the end of the chants" | `dotField()`: fifteen equal slices of the walk-out's first twelve seconds, the k-th holding F(k) dots — 1, 1, 2, 3 … 610 — 1,596 sung dots from the three of them, pitched a little off, spun on the golden angle, the last slice a buzz 1.3 ms apart. Then the `burst` at bar 110: the sharpest impact on the record, two bars before the click. Its own generator. |
| 32 | "remove the first bang at the start of the track and move the other one to the start of the first 'dash'" | One waterhole strike now, on the downbeat of bar 29 (the first sung dash), at the opening's full weight. None at bar 8, none at bar 31. |
| 33 | "trappy trippy chromatically arpeggiating hi hats during the hum / chant section" | `trapHats()`: 16ths with 32nd rolls in the last beat and a triplet burst every other pair of bars, every hit one semitone above the last, climbing on even bars and falling on odd. Under the secret act (0.62) and the first six bars of the walk-out (fading). |

Both engines sample-exact after all of it (1.2e-7). Events 4,179 → 6,118.

---

# Round seven

| # | note | what was done |
|---|---|---|
| 34 | "after the long wanna there is no more 'dasssssh' from jeffrey, but i want one … more normal length, start after wanna ends" | In the slow stack Jeffrey keeps his 1.5 s hold take and waits 1.20 s — the long wanna ends, then he says dash — on the tube bus as before AND sung on the vox bus (centred, dry, shelved, arc darkness declined) so the kick pump cannot bury him. Camille and Alex stay long from the downbeat. |
| 35 | "the kick should pitch down in the cult chants" | `kick()` grew a `pitch`; the chant kicks sit at 0.80 of the record's kick and sink to 0.70 across each act. |
| 36 | "the cult chants themselves should change pitch over time, have it oscillate" | The choir's 7-cent shimmer became a slow sway of most of a semitone (62/80/98 cents at 0.16/0.22/0.28 Hz), each voice at its own rate. |
| 37 | "around 0:55 all voices should distance themselves … more about the instruments for a good 20 seconds … a little tour of the orchestra, spatially" | Source bars 54.5–63.9 (0:49–1:08 after the cut): the vox and tube buses fall to a third, the side return to 60%, and the music bus takes one full turn around the listener before the voices come back. |
| 38 | "around 1:20 some sound fx of bubbles" | `bubbles()` — 52 pops across bar 74–76 (1:14–1:17 after the cut), each a sine rising 1.6× over 45 ms and gone in 80, at its own pitch, time and place. Own generator. |
| 39 | "slow down the fibonacci thing so it lasts a bit longer, repeats in reverse in and out, pitching around, slowing down a bit" | Forward 12 → 13 s from bar 104; the burst moved to 110.5; then the field runs back down 610 → 1 over 3.6 s with every slice 12% longer than the last and the pitch sinking six semitones, ending on the click. |
| 40 | "remove a few bars that are too repetitive, bring the track down to like the 2 minute mark" | `cut-release.sh`: bars 37–40 out (the third hook in a row) and bars 96–104 out (the recognition act, a fourth statement of the chorus). Seams on downbeats. 2:18.85 → **1:56.85**. |

Both engines sample-exact (1.2e-7).

---

# Round eight (the remaster)

| # | note | what was done |
|---|---|---|
| 41 | "make the vocals sit in the mix more, especially those dashes we just added" | The slow stack no longer lifts 18%: it sits 8% under the plain one, wider (side 0.82) and wetter (dly 0.24); Jeffrey's vox double 0.55 → 0.36 with more room; the whole vox bus VOXG 1.42 → 1.28. |
| 42 | "lets make things feel really felt and played" | Humanization widened with the draw count untouched: scatter 1.7 → 2.3, velocity spread 1.35 → 1.7, the phrase breathing 35% deeper. Every hit still lands on the same draw; it lands looser. |
| 43 | "the bubbles should be fading in, pitching around more, fucked and chopped more" | Each pop's level follows its place in the cluster (squared, so the first are barely there); pitch drawn across 250–2400 Hz, gliding down as easily as up; a third of them stutter two to four times, 18–30 ms apart, each softer. |
| 44 | "can the dash after the long 'wanna' be in the same pitch as the wanna, so it sounds more composed" | The long wanna is sung D4 → E4; on those hooks the dash that follows is E4 (Camille and Alex at E4, Jeffrey at E3, his own octave). |
| 45 | "okay this is nice lets remaster now … fit the thing into our pop master" | `cut-release.sh`'s density stage is parametrized (`GAIN2`, `LIMIT`) and set by measurement against `pop/MASTERING.md`: GAIN2 5.6 dB into a 0.760 ceiling → **−10.0 LUFS · LRA 6.0 · −2.3 dBTP**. House gate: PLR 7.7, mono fold −0.1 dB, phone proxy −2.9 dB. |

Pending: a physically modelled strum machine (`pop/guitar/`) and a guitar critique are being built by a subagent; the guitar will then be reorchestrated — more complex, tied to the track's existing relationships, folky and rocky under Jeffrey's first lines.

---

# Round nine

| # | note | what was done |
|---|---|---|
| 46 | "some of jeffrey's long dashes, around 0:45, quantized into lower bitrates … as they trail off, like the matrix entering the vocal … 'bittune', like autotune but for bitrate … aware of the percussive timing of the 16th step" | `bitTune` on `shot()`: the take starts at 14 bits and, on every 16th of the RECORD's grid (absolute time, so the steps land with the hats), loses 1.5 bits and holds each sample one step longer, down to 4 bits held 8 samples at a time. Jeffrey's dash in every slow stack — the tube layer and the vox double — takes it; his sub floor does not. |
| 47 | "when the dot fibonacci ends it should be like a firework dot burst … a particle-based explosion" | `firework()`: 140 particles launched inside 0.3 s at bar 110.5, each starting near the centre and landing at its own edge (later launches fly further), rising in pitch to an apex and falling, quieter and wetter as it drifts; a third of them crackle three or four times. Then the reversed field rains down as the debris. |
| 48 | "around 0:22 camille's dot dot dot trail could fibonacci out a bit, or fractal out" | `dotTail()`: after the hook's four ending dots and after every message-act scale run, more dots climbing her scale with every gap the golden ratio of the last — 0.5, 0.31, 0.19, 0.12, 0.07 … — each quieter by the same ratio, until a flutter and then nothing. |

Both engines sample-exact (1.5e-7).

---

# Round ten

| # | note | what was done |
|---|---|---|
| 49 | "the bit crunching should last longer, more drawn out, less harsh, it should sound like the voice is turning into a square wave from its natural form, play then reverse, pitch around — not the same pitch or length each one — and smoother" | Bittune redesigned: no sample-rate decimation (that was the harshness); the bits go 0.6 per 16th from 16 down to 2 — where a voice IS a square wave — with a one-pole closing as they go, so the steps round off as they grow. Each slow hook's dash takes its own transposition and length (0/1.5 s, +2/1.15, −3/1.85, +5/1.3), and then plays back REVERSED, a quarter quieter, starting from 6 bits. New `reverse` on `shot()` and `staircasePan()`. |
| 50 | "some of alex's slow CULT samples around 1:16 do CULT-TLUC, like reverse itself" | The bridge staircase cults are followed by themselves backwards, a fifth down like their forward selves, starting as the word ends and walking the other way. Two of the six in the 1:25 heap also say it backwards. |
| 51 | "maybe some accordion at 0:34" | One G bellows opened slowly across the bar-47 line (the last breath of the turn before the reply), and its opposite-bellows answer a bar later. |
| 52 | "with a subagent make a guitar strum machine … then critique our use of guitar … then redo / reorchestrate the guitar musically, more complex, string together the technical elements, play off existing relationships, folky and rocky under jeffrey's initial sayings" | `pop/guitar/c/strum` (six waveguide strings on one bridge, a hand with down/up/chuck strokes, dreadnought body or the cult amp) and `GUITAR-CRITIQUE.md`. Then the reorchestration: 33 takes rendered into `samples/gt-*.wav` and placed ON THE GRID, chord following the BED (`degAt`) instead of a cycle of its own — folky acoustic under the first hooks (29–36), palm-muted electric on the bed's chord through the reply (the pedal where the bed is Bm, a chuck on every last 16th), the four-bar acoustic flower phrase at 64, one quiet up-stroke per dot-field bar, the finale hand electric every bar with the chuck where the hole was, a real down-stroke at 31, a hand before the vocal at 9. Retired: `guitar-chug`, `guitar-wide` and its double, the KS block chords and the KS finale hand (three tunings and three fuzz stages of one voicing). The KS single-note lines stay, with the half-sample delay compensated (the shreds were 16 cents flat). |

Engine parity: 5e-5 (−86 dBFS), all of it inside the bittuned dashes — a 2-bit quantizer amplifies last-ulp differences between V8's and libm's `pow` into occasional one-step flips. Everything else remains at 1e-7. Inaudible; documented.

---

# Round eleven

| # | note | what was done |
|---|---|---|
| 53 | "there isn't enough deep scary air … in like 0:20–0:30, during the cult hums" | A third layer in `air()`: the noise through two one-poles at ~45 Hz, a rumble with almost nothing above 100 Hz, on its own slower wander. Under the turn's tiles (bars 40–48) it runs at 2.2× depth and the whole bed 40% up. |
| 54 | "power saws around 0:45 for some of those vocals to back them" | `powerSaw()`: seven detuned saws (−14 … +14 cents) into a one-pole that opens over the note. F#3+F#2 under the first dash of the 0:45 hook, E3+E2 under the slow one. |
| 55 | "the final scratch dot dot dot i don't like — i was hoping more of a poppy explosion when the fibonacci ended" | The reversed debris field is gone. The firework is tighter (launch window 0.18 s), brighter and denser (220 particles), with three pitched pops — the kick at 2.4/2.9/3.4× — cracking on top. The pops were first set loud enough to spike the record's single linear trim by 3.6 dB; brought down, and the master's GAIN2 5.6 → 6.6 dB to hold −10 LUFS with the limiter taking the transient instead of the whole record paying for it. |
| 56 | "less layered vocals would be best around 0:40" | `thin` on `chorus()`: the reply's first statement loses the sub floor under "hideaway" and its line-3 dash stack stays the plain three voices instead of the slow, doubled, reversed one. |

---

# Round twelve

| # | note | what was done |
|---|---|---|
| 57 | "humanize the rhythm and placement now" | FEEL: every bus sits a few ms off the clock in its own direction — hats 4 ms ahead, snare 7 behind, bass 5, music 3, the voices 10, the tube dashes 12 — and the amount breathes over seven bars so it is never the same lateness twice. The kick stays the clock. Draw-free. |
| 58 | "add a swing in some sections, especially as we move towards the end — more and more of a serious swing" | SWING: the off-eighths and off-sixteenths land late by a share of the gap — 0 through the message, 8% from the reply, 16% where it spreads, 26% through the whole message, 34% in the walk-out (2:1 triplet is 33%). Applied to hats, rolls, rides, tambourine, taps, blocks, the drive, the trap hats, the bass's off-eighths, the stabs, and the hook's "i wanna" and dot offbeats. |
| 59 | "at 1:49 should be the dot explosion — it seems to come too slow, or the dots get so small they are inaudible; check the fibonacci math" | The math was wrong: fifteen slices over 13 s put 610 dots in the last 0.87 s — 1.4 ms apart, cut to 2 ms grains at a tenth of the gain: an inaudible buzz that faded instead of arriving. Thirteen terms now (…233) over 9 s, no grain shorter than 40 ms so the dense slices overlap into a roar, gain falling with the fourth root of the count instead of the square root. The pop moved from bar 110.5 to 108.5 — **1:49** on the shipped edit. |

Master re-measured at GAIN2 6.0 after the swing changed the density.

---

# Round thirteen

| # | note | what was done |
|---|---|---|
| 60 | "the kick drum should get sharper and evolve around the swing too" | `sharp` on `kick()`: grows the click 2.6×, quickens the slam, shortens the tail. The score feeds it a 25% baseline plus the swing amount, so the kick tightens as the record swings harder — and swung ghost kicks appear on the "and" of 4 from the reply, and the "and" of 2 once the swing is serious, late by the swing. |
| 61 | "the perc should get more cloppy, snares more clipping — those forms can switch out" | Three forms of the wooden tap: plain; CLOP (body resonances up at 1400/620 Hz, a longer stroke, a hollow tock falling 1250 → 900 Hz under the wood); CLIP (driven 3.2× into a hard ±0.55). They rotate every four bars — plain/clop/clip through the message, clop/clip once the swing is in. The taps go cloppy on the clop bars. The noise stream's draw count never changes with the form. |
| 62 | "the transition to the swing feels a bit awkward around 1:07 — be more sensitive to the smoothness of the rhythm" | The swing is a continuous smoothstep now — 0 at bar 44 to the full 34% at 104, about half a percent a bar — instead of steps per act. The feel and the kick's sharpness derive from it, so they ramp with it. |
| 63 | "as the swing is added we pick up the bpm even, so we can keep up the energy" | `bin/tempo.py`: the finished pre-master goes through rubberband-r3 (pitch preserved) on a time map whose tempo follows the swing's own curve — 120 through bar 44, a smoothstep to 128 by bar 104 — and the release edit's trim points are mapped through the same warp. Part of `bake-c.sh` now (+~5 s). 1:56.85 → **1:54.13**. |
