A single oversized command-line text prompt rendered as one solid physical object: a short horizontal baseline ending in a chunky blinking block cursor, a fat rectangular caret with crisp pixelated corners, caught mid-blink so it glows faintly. Just before the cursor sits one small round dot in a warm magenta-pink, the prompt's signature pip, like a tiny enamel bead resting on the line. The whole thing reads like a pressed seal or rubber stamp of a terminal prompt — a lone glyph, no words, no letters spelled out, only the bare blinking caret and its pink dot floating on the baseline. Treat it as a musical-instrument key: weighty, tactile, inviting a press.
