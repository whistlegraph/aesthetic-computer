<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Main (shared) AndroidManifest - minimal base for both flavors
    Flavor-specific manifests in src/consumer/ and src/kiosk/ add their permissions and components
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Base permission needed by both flavors -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AestheticComputer"
        tools:targetApi="31">
        
        <!-- Activity and other components defined in flavor manifests -->

    </application>
</manifest>