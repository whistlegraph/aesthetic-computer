# heavy media — reproducible from URL via grab.mjs
*.mp4
*.wav
*.m4a
# keep the small analysis + index
!*.analysis.json
!INDEX.json
# per-id transcripts live in TRANSCRIPTS.json; audio cache is heavy
transcripts/
video/
glyphs/
# resumable per-post vision results and optional contact sheets are machine-local;
# visual-farm.mjs compiles them into the tracked VISUALS.json artifact
visual-records/
visual-montages/
# intermediate naming work-list (regenerated from SONGS.json)
NAMING.json
# noisy YouTube search review queue (regenerated; mostly not-ours)
YOUTUBE-CANDIDATES.json
# upload staging (rebuilt from glyphs/ + video/)
site/
# Longest Whistlegraph film + stills — pulled from Dropbox/YouTube, on the CDN
longest/
naming-batches/
