# heavy media — reproducible from URL via grab.mjs
*.mp4
*.wav
*.m4a
# keep the small analysis + index
!*.analysis.json
!INDEX.json
# per-id transcripts live in TRANSCRIPTS.json; audio cache is heavy
transcripts/
video/
glyphs/
# intermediate naming work-list (regenerated from SONGS.json)
NAMING.json
# noisy YouTube search review queue (regenerated; mostly not-ours)
YOUTUBE-CANDIDATES.json
# upload staging (rebuilt from glyphs/ + video/)
site/
