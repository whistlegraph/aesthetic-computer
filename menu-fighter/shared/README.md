# Shared product contracts

This directory is reserved for platform-neutral contracts once they need to be
consumed outside Aesthetic Computer. Do not copy the fight simulation here.
Extract it atomically, with all fight specs green before and after the move.

Likely first modules: binary packet codec, match handshake schema, transport
interface, replay format, and content/build hashing.
