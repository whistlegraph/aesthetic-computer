Installing `video-sync` on a Raspberry Pi 2:

  0. `cd` into this directory.

  1. Install NodeJS
    `curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -`
    `sudo apt install nodejs`
    
  2. Install dependencies
    `npm install`